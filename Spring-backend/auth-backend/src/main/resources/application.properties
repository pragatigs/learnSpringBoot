spring.application.name=shopify

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/shopify_db?serverTimezone=UTC
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=postgres

server.port=8080

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Secret Key (must be at least 256 bits / 32 characters for HS256)
JWTsecretKey=24Jan2026MySecureJWTSecretKey4Auth!@#$%^&*()_+
